{
  "name": "php-inspections",
  "description": "Code inspection tool for PHP",
  "version": "0.0.1",
  "license": "MIT",
  "engines": {
    "node": "*"
  },
  "bin": "./dist/bin/php-inspections.js",
  "dependencies": {
    "php-parser": "^0.1.4"
  },
  "devDependencies": {
    "typescript": "^2.1.0",
    "typings": "^2.1.0"
  },
  "scripts": {
    "prepublish": "typings install && tsc",
    "watch": "./node_modules/.bin/tsc",
    "run": "./node_modules/.bin/ts-node bin/php-inspections.ts"
  }
}
